ci:
    autofix_prs: false
    autoupdate_schedule: weekly

repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: "v0.1.13"
    hooks:
      # Format the code
      - id: ruff-format
      # Lint the code
      - id: ruff
        # args: [ --fix ]